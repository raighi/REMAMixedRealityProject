using UnityEngine;
using UnityEngine.UI;
/*

public class EndingInteractions : MonoBehaviour
{
    [Header("UI Component")]
    public Button nextButton;

    [Header("App Controller")]
    private EndingController introController;

    public void Initialize(EndingController controller)
    {
        introController = controller;
    }

    private void OnEnable()
    {
        nextButton.onClick.AddListener(OnNextPressed);
    }

    private void OnDisable()
    {
        nextButton.onClick.RemoveListener(OnNextPressed);
    }

    private void OnNextPressed()
    {
        introController.ProceedIntro();
    }
}*/